<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Spacing (same as light theme) -->
    <dimen name="spacing_tiny">4dp</dimen>
    <dimen name="spacing_small">8dp</dimen>
    <dimen name="spacing_medium">16dp</dimen>
    <dimen name="spacing_large">24dp</dimen>
    <dimen name="spacing_xlarge">32dp</dimen>

    <!-- Text Sizes (same as light theme) -->
    <dimen name="text_size_small">12sp</dimen>
    <dimen name="text_size_medium">14sp</dimen>
    <dimen name="text_size_large">16sp</dimen>
    <dimen name="text_size_xlarge">18sp</dimen>
    <dimen name="text_size_xxlarge">20sp</dimen>
    <dimen name="text_size_title">24sp</dimen>

    <!-- Corner Radius (same as light theme) -->
    <dimen name="corner_radius_small">4dp</dimen>
    <dimen name="corner_radius_medium">8dp</dimen>
    <dimen name="corner_radius_large">12dp</dimen>
    <dimen name="corner_radius_xlarge">16dp</dimen>

    <!-- Elevation (same as light theme) -->
    <dimen name="elevation_low">2dp</dimen>
    <dimen name="elevation_medium">4dp</dimen>
    <dimen name="elevation_high">8dp</dimen>

    <!-- Icon Sizes (same as light theme) -->
    <dimen name="icon_size_small">16dp</dimen>
    <dimen name="icon_size_medium">24dp</dimen>
    <dimen name="icon_size_large">32dp</dimen>

    <!-- Button Sizes (same as light theme) -->
    <dimen name="button_height">48dp</dimen>
    <dimen name="button_padding">12dp</dimen>

    <!-- Toolbar (same as light theme) -->
    <dimen name="toolbar_height">56dp</dimen>

    <!-- Navigation Drawer (same as light theme) -->
    <dimen name="nav_header_height">160dp</dimen>
    <dimen name="nav_drawer_width">280dp</dimen>

    <!-- Card View (same as light theme) -->
    <dimen name="card_elevation">4dp</dimen>
    <dimen name="card_padding">16dp</dimen>

    <!-- List Item (same as light theme) -->
    <dimen name="list_item_height">56dp</dimen>
    <dimen name="list_item_padding">16dp</dimen>

    <!-- EditText (same as light theme) -->
    <dimen name="edittext_height">48dp</dimen>
    <dimen name="edittext_padding">12dp</dimen>

    <!-- Divider (same as light theme) -->
    <dimen name="divider_height">1dp</dimen>

    <!-- Progress Bar (same as light theme) -->
    <dimen name="progress_bar_height">4dp</dimen>
    <dimen name="progress_bar_width">200dp</dimen>

    <!-- Bluetooth Specific (same as light theme) -->
    <dimen name="bluetooth_device_item_height">64dp</dimen>
    <dimen name="bluetooth_status_indicator">12dp</dimen>

    <!-- Log View (same as light theme) -->
    <dimen name="log_item_height">40dp</dimen>
    <dimen name="log_view_height">200dp</dimen>
</resources>